      <table id="trainingSessionList" class="table table-striped table-bordered table-condensed">
            <thead>
                <tr class="">

                    <th style="width: 40px;">
                    </th>
                    <th>
                        <v-sorter text="Session Date" member="TrainingDate"
                                  grid-state="request"
                                  onsort="refresh()"></v-sorter>

                    </th>
                    <th>
                        <v-sorter text="Course" member="TrainingCourse.Name"
                                  grid-state="request"
                                  onsort="refresh()"></v-sorter>
                    </th>
                    <th>
                        <v-sorter text="Trainer" member="Trainer.LastName, Trainer.FirstName"
                                  grid-state="request"
                                  onsort="refresh()"></v-sorter>

                    </th>
                    <th>
                        <v-sorter text="Participants" member="TrainingSessionAttendances.Count()"
                                  grid-state="request"
                                  onsort="refresh()"></v-sorter>
                    </th>
                    <th>
                        <v-sorter text="Is Active" member="isActive"
                                  grid-state="request"
                                  onsort="refresh()"></v-sorter>
                    </th>
                    <th style="width: 40px;">
                    </th>

                </tr>
            </thead>
            <tbody ng-repeat="group in data">
                <tr>
                    <td colspan="7" class="info">
                        <a class="btn"
                           ng-show="request.hidden.indexOf(group.id) != -1"
                           ng-click="show(group.id)" title="show"><i class="glyphicon glyphicon-plus-sign"></i></a>
                        <a class="btn"
                           ng-show="request.hidden.indexOf(group.id) == -1"
                           ng-click="hide(group.id)"
                           title="hide"><i class="glyphicon glyphicon-minus-sign"></i></a>

                        <b>{{group.name}}</b>
                    </td>
                </tr>
                <tr ng-repeat="item in group.data" ng-show="request.hidden.indexOf(group.id) == -1">
                    <td class="buttons">
                        <a class="btn" ng-href="TrainingSession/Update/{{item.id}}" title="update"><i class="glyphicon glyphicon-update"></i></a>
                    </td>
                    <td>
                        <label>{{item.lastTrainingDate | date:"MM-dd-yyyy"}}</label>
                    </td>
                    <td>
                        <label>{{item.courseName}}</label>
                    </td>
                    <td>
                        <label>{{item.trainerLastName}}, {{item.trainerFirstName}}</label>
                    </td>
                    <td>
                        <label>{{item.countOfParticipants}}</label>
                    </td>
                    <td>
                        <input type="checkbox" disabled="disabled" ng-checked="{{item.isActive}}" />
                    </td>
                    <td class="buttons">
                        <a class="btn" ng-show="item.pass" ng-href='Report/CompletedTraining/{{item.id}}' title="certificates"><i class="glyphicon glyphicon-print"></i></a>
                    </td>
                </tr>
            </tbody>
        </table>
  
