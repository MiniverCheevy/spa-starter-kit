add sql scripts as embedded resources here, the will be run when database is updated

scripts should be structured in a way that they can be run over and over with out errors

There should always be an up to date script with both ef migrations and the scripts in the tools folder

