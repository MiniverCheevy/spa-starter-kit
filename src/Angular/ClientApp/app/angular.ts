import { Router, ActivatedRoute } from '@angular/router';
import { Component, Output, Input, EventEmitter, Injectable } from '@angular/core';
import { OnInit, OnChanges, AfterViewChecked, AfterContentChecked, DoCheck } from '@angular/core';
import { NgForm } from '@angular/forms';

export {
    Router, ActivatedRoute,

    Component, Output, Input, EventEmitter,

    OnInit, OnChanges, AfterViewChecked, AfterContentChecked, DoCheck,

    Injectable,

    NgForm
}

